# 今日印钞 v1.0.1

一个维护更新版本，主要补齐发布流程和下载入口。

## 更新

- 新增一键发布脚本，可同时发布到 GitHub 和 Gitee Release
- README 增加最新版本下载入口，方便直接获取最新版
- 发布脚本会在发版时自动同步 README 里的下载链接
- 完善本地打包与发布说明，减少手工发布步骤

## 使用方式

1. 下载 `今日印钞.app` 或对应压缩包
2. 打开后在菜单栏中找到 `今日印钞`
3. 点击菜单栏图标进入设置面板
4. 填写你的月薪、时薪、工时和规则

## 已知说明

- 当前主要面向 `macOS 14+`
- 如果遇到 Gatekeeper 拦截，需要在系统设置中允许打开
- 命令行构建依赖本机已安装匹配版本的 Command Line Tools
